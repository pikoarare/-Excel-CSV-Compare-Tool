# -Excel-CSV-Compare-Tool
## Overview  This tool allows you to compare two Excel or CSV files and extract differences.  - Added rows - Removed rows - Changed values  ExcelやCSVファイルを比較し、差分（追加・削除・変更）を抽出できるツールです。
